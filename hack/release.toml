# commit to be tagged for the new release
commit = "HEAD"

project_name = "sbc-allwinner"
github_repo = "siderolabs/sbc-allwinner"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
